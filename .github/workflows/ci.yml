name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:          
      - name: Check out
        uses: actions/checkout@v2
      
      - name: Docker Login
        uses: docker/login-action@v1.8.0
        with:
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_PASSWORD }}

      - name: Build Images
        run: |
          docker build kubeflow_pipeline/0_data -t kosehy/mnist-pre-data
          docker push kosehy/mnist-pre-data
          docker build kubeflow_pipeline/1_validate_data -t kosehy/mnist-val-data
          docker push kosehy/mnist-val-data
          docker build kubeflow_pipeline/2_train_model -t kosehy/mnist-train-model
          docker push kosehy/mnist-train-model
          docker build kubeflow_pipeline/3_embedding -t kosehy/mnist-embedding
          docker push kosehy/mnist-embedding
          docker build kubeflow_pipeline/4_train_faiss -t kosehy/mnist-train-faiss
          docker push kosehy/mnist-train-faiss
          docker build kubeflow_pipeline/5_analysis_model -t kosehy/mnist-analysis
          docker push kosehy/mnist-analysis
          docker build kubeflow_pipeline/6_deploy -t kosehy/mnist-deploy
          docker push kosehy/mnist-deploy
      
      - name: Slack Notification
        if: always()
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_ICON_EMOJI: ':bell:'
          SLACK_CHANNEL: test
          SLACK_MESSAGE: 'Build/Push Images :building_construction: - ${{job.status}}'
          SLACK_USERNAME: kosehy
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}
